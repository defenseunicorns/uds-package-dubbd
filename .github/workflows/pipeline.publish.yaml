name: Publish DUBBD Packages

on:
  push:
    tags:
      - "v*"  

permissions:
  contents: read
  packages: write

jobs:
  publish-packages:
    runs-on: ubuntu-latest
  
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Zarf
        uses: defenseunicorns/setup-zarf@main
        with:
          # renovate: datasource=github-tags depName=defenseunicorns/zarf versioning=semver
          version: v0.26.2
          download-init-package: true

      - name: Login to Registry1
        uses: docker/login-action@v2
        with:
          registry: registry1.dso.mil
          username: ${{ secrets.REGISTRY1_USERNAME }}
          password: ${{ secrets.REGISTRY1_PASSWORD }}

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: dummy
          password: ${{ secrets.GITHUB_TOKEN }}
    
      - name: Create DUBBD
        run: zarf package create --confirm
        working-directory: defense-unicorns-distro
        timeout-minutes: 60

      - name: Create DUBBD-AWS
        run: zarf package create --confirm
        working-directory: aws
        timeout-minutes: 60
      
      - name: Publish DUBBD
        run: zarf package publish zarf-package-big-bang-amd64-*.tar.zst oci://ghcr.io/defenseunicorns/packages
        working-directory: defense-unicorns-distro
        timeout-minutes: 60

      - name: Publish DUBBD-AWS
        run: zarf package publish zarf-package-*.tar.zst oci://ghcr.io/defenseunicorns/packages
        working-directory: aws
        timeout-minutes: 60
